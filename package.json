{
  "name": "ascii-art-generator-web-tools",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build:web": "node scripts/build-web.mjs",
    "serve:web": "node scripts/serve-web.mjs",
    "test:web": "npm run build:web && playwright test"
  },
  "devDependencies": {
    "@playwright/test": "1.61.1"
  }
}
